import styled from 'styled-components';

const Tabs = styled.View`
  display: flex;
  flex: 1;
  flex-direction: row;
  justify-content: space-evenly;
  align-items: center;
  margin-top: 25;
  margin-bottom: 25;
`;

export default Tabs;
